# PRD 使用说明

本目录包含完整的产品需求文档，可以独立分享给产品团队。

## 文件说明

- **PRD.md**: 主文档，包含所有需求信息（自包含，可独立阅读）
- **assets/images/**: 所有流程图、实体图、原型图的渲染图片（PNG）⭐
- **assets/mermaid/**: 所有图表的 Mermaid 源码（.mmd 文件）⭐
- **assets/diagrams/**: 交互式流程图（HTML，可选）

## 如何使用

### 📖 阅读 PRD

推荐使用支持 Markdown 的工具：
- **GitHub/GitLab**: 自动渲染图片和折叠块
- **VS Code**: Markdown Preview Enhanced 插件
- **Typora**: 所见即所得编辑器

### 🎨 编辑图表

如果需要修改流程图或实体关系图：

1. **在线编辑**（推荐）
   ```bash
   # 打开对应的 .mmd 文件，复制内容
   # 访问 https://mermaid.live
   # 粘贴代码，在线编辑
   ```

2. **本地编辑**
   ```bash
   # 直接编辑 assets/mermaid/*.mmd 文件
   # 重新生成 PRD
   npm run generate-prd
   ```

3. **VS Code 编辑**
   ```bash
   # 安装插件：Mermaid Preview
   # 打开 .mmd 文件，实时预览
   ```

### 📤 导出 PDF

使用 Markdown 转 PDF 工具：
```bash
# 方式1: 使用 md-to-pdf
npx md-to-pdf PRD.md

# 方式2: 使用 Typora
# 打开 PRD.md，选择 文件 -> 导出 -> PDF
```

### 📦 分享给团队

**方式1: 打包整个目录**
```bash
# Windows PowerShell
Compress-Archive -Path PRD -DestinationPath PRD.zip

# macOS/Linux
zip -r PRD.zip PRD/
```

**方式2: 使用 Git**
```bash
# 提交到仓库，团队成员可以 clone 查看
git add docs/PRD/
git commit -m "docs: 生成产品需求文档"
```

**方式3: 导出为 PDF**
```bash
npx md-to-pdf PRD.md
# 只需分享 PRD.pdf 文件
```

## 📊 图表源码管理

所有图表的 Mermaid 源码都保存在 `assets/mermaid/` 目录：

| 图表类型 | 文件命名规则 | 示例 |
|---------|-------------|------|
| 业务实体关系图 | `business-entity-diagram.mmd` | business-entity-diagram.mmd |
| 模块关系图 | `module-relationship-N.mmd` | module-relationship-0.mmd |
| 业务流程图 | `flow-{模块名}-N.mmd` | flow-cli-init.mmd |

**为什么保留源码？**
- ✅ 便于后续修改和维护
- ✅ 可以在不同工具中打开编辑
- ✅ 版本控制友好（纯文本）
- ✅ 支持团队协作编辑

## 🔧 重新生成 PRD

如果你修改了需求文档或图表源码，可以重新生成 PRD：

```bash
# 在 Claude Code 中输入
生成PRD

# 或使用命令行（待实现）
ai-dev generate-prd
```

## 📝 文档结构

```
PRD/
├── PRD.md                              # 主文档
├── README.md                           # 本文件
└── assets/
    ├── images/                         # 渲染图片
    │   ├── business-entity-diagram.png
    │   └── flow-cli-init.png
    └── mermaid/                        # 源码文件
        ├── business-entity-diagram.mmd
        └── flow-cli-init.mmd
```

## ⚠️ 注意事项

1. **图片引用**: PRD.md 中使用相对路径引用图片，不要移动文件位置
2. **编码格式**: 所有 .mmd 文件使用 UTF-8 编码
3. **版本控制**: 建议将整个 PRD/ 目录纳入 Git 管理
4. **更新同步**: 修改需求文档后，记得重新生成 PRD

## 📚 更多资源

- [Mermaid 官方文档](https://mermaid.js.org/)
- [Mermaid Live Editor](https://mermaid.live)
- [Markdown Guide](https://www.markdownguide.org/)

---

**生成时间**: 2025-12-10
**生成工具**: AI 超级个体开发助手 + PRD Generator (手动验证版本)
**文档版本**: v1.0
